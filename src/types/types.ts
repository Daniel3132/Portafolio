export type ContactItem = {
    name: string;
    url: string;
    img: string;
}

export interface Language {
    name: string;
    url: string;
}

export type Project = {
    name: string;
    description: string;
    photo: string;
    repository: string;
    deployment: string;
};
